from setuptools import setup, find_packages

setup(
    name="wrong_material_detector",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "pandas",
        "pygame",
        "pyttsx3"
    ]
) 